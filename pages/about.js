import React from "react";
import styles from "../styles/About.module.scss";

const about = () => {
  return (
    <div
      className={`${styles.about} d-flex justify-content-center align-items-center fs-1`}
    >
      About
    </div>
  );
};

export default about;
