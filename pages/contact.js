import React from "react";
import styles from "../styles/Contact.module.scss";

const contact = () => {
  return (
    <div
      className={`${styles.contact} d-flex justify-content-center align-items-center fs-1`}
    >
      Contact
    </div>
  );
};

export default contact;
